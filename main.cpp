

#include <list>
#include <iostream>
#include "list/list.hpp"

int main()
{
	ft::list <int> lst(3);
	// std::list<int>::iterator it(lst.begin());
	// ft::list<int> lst1;
	// it = lst.begin();
	// std::list<int>::pointer ptr(lst.begin);
	// std::list<int>::pointer ptr;
	// ft::list<int>::pointer ptr1;
	// std::list<int>::iterator it = lst.begin();
	// ptr = &*it;
	// ptr1 = &*it;
	// // std::list <int>::reference p = al;
	// std::cout << ptr << std::endl << ptr1 << std::endl;

}